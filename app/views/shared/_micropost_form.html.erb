<%= form_for(@micropost) do |f| %>
<!--object: f.objectはerror_messagesパーシャルの中でobjectという変数名を作成してくれる-->
<!--(fの値が@userでも@micropostでもエラーメッセージが機能するようになる) -->
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "例...
メーカー: ○○○
フレーバー: ○○○
1kgあたりの価格: ○○○円
レビュー: カップ一杯で約20gのタンパク質が取れます。また水に溶けやすくほとんどダマになりません。
でもバニラ味は少し甘すぎるので私はプレーンと半々で割って飲んでます。" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
  <span class="picture">
    <%= f.file_field :picture, accept: 'image/jpeg,image/gif,image/png' %>
  </span>
<% end %>

<!--ファイルサイズをjQueryでチェック-->
<script type="text/javascript">
  $('#micropost_picture').bind('change', function() {
    var size_in_megabytes = this.files[0].size/1024/1024;
    if (size_in_megabytes > 5) {
      alert('Maximum file size is 5MB. Please choose a smaller file.');
    }
  });
</script>